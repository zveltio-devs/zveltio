{
  "_what": "Tabele fără `tenant_id`, declarate deliberat ca fiind de instanță. Lista poate scădea; o intrare nouă înseamnă o decizie, nu o omisiune.",
  "_why": "Prefixul nu spune de care parte e o tabelă, iar o tabelă fără `tenant_id` arată IDENTIC dacă a fost decizie sau scăpare. Ambiguitatea a costat deja de două ori: o poartă care a raportat trei bucăți de cod corect drept violări, și premisa falsă a unui branch întreg.",
  "_how_to_fix": "Dacă tabela e per firmă, dă-i `tenant_id` (și politica RLS). Dacă e de instanță, adaugă-o aici CU motivul. Un rând marcat NEVERIFICAT nu e o justificare, e o sarcină.",
  "_regenerate": "bun run scripts/check-tenant-boundary.ts --list",
  "_children_of_tenant_tables": "Nouă dintre ele referă un părinte per-firmă. Nu sunt scurgeri dovedite — sunt izolare fără a doua linie.",
  "instance_level": {
    "account": "VERIFICAT 2026-09-02: nicio coloană `tenant_id` (catalog). Identitatea e la nivel de instanță prin proiectare — firma se rezolvă PER CERERE din antet în `tenantMiddleware` (`middleware/tenant.ts:129`, `X-Tenant-Slug` → subdomeniu → env → implicit), nu se ține pe sesiune. De asta un utilizator comută firma fără să se reautentifice, iar apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id`. Atașat lui `user`.",
    "passkey": "VERIFICAT 2026-09-02: nicio coloană `tenant_id` (catalog). Identitatea e la nivel de instanță prin proiectare — firma se rezolvă PER CERERE din antet în `tenantMiddleware` (`middleware/tenant.ts:129`, `X-Tenant-Slug` → subdomeniu → env → implicit), nu se ține pe sesiune. De asta un utilizator comută firma fără să se reautentifice, iar apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id`. Credențial al utilizatorului.",
    "session": "VERIFICAT 2026-09-02: nicio coloană `tenant_id` (catalog). Identitatea e la nivel de instanță prin proiectare — firma se rezolvă PER CERERE din antet în `tenantMiddleware` (`middleware/tenant.ts:129`, `X-Tenant-Slug` → subdomeniu → env → implicit), nu se ține pe sesiune. De asta un utilizator comută firma fără să se reautentifice, iar apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id`.",
    "twofactor": "VERIFICAT 2026-09-02: nicio coloană `tenant_id` (catalog). Identitatea e la nivel de instanță prin proiectare — firma se rezolvă PER CERERE din antet în `tenantMiddleware` (`middleware/tenant.ts:129`, `X-Tenant-Slug` → subdomeniu → env → implicit), nu se ține pe sesiune. De asta un utilizator comută firma fără să se reautentifice, iar apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id`. Credențial al utilizatorului.",
    "user": "VERIFICAT 2026-09-02. Un utilizator poate fi membru în mai multe firme; apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id` (confirmat în catalog). O politică aici ar face imposibil login-ul înainte de a alege o firmă.",
    "verification": "VERIFICAT 2026-09-02: nicio coloană `tenant_id` (catalog). Identitatea e la nivel de instanță prin proiectare — firma se rezolvă PER CERERE din antet în `tenantMiddleware` (`middleware/tenant.ts:129`, `X-Tenant-Slug` → subdomeniu → env → implicit), nu se ține pe sesiune. De asta un utilizator comută firma fără să se reautentifice, iar apartenența trăiește în `zv_tenant_users`, care ARE `tenant_id`. Jetoane emise înainte să existe o firmă curentă.",
    "zv_api_key_access_log": "**Copil al lui `zv_api_keys`, fără `tenant_id` propriu.** VERIFICAT 2026-09-02: singurul sit (`routes/admin/system-routes.ts:252`) filtrează prin părinte cu `AND api_key_id IN (SELECT id FROM zv_api_keys WHERE tenant_id = …)`, cu motivul scris deasupra. Corect.",
    "zv_audit_log": "VERIFICAT 2026-09-02. Ca și jurnalele de observabilitate: **poartă activitatea fiecărei firme** (`user_id`, `resource_id`, `resource_type`, `metadata`, `ip`) fără `tenant_id`, deci RLS n-are pe ce se lega. Cititorii sunt doar `system-routes.ts:534,563`, sub `adminRoutes`, a cărei gardă globală `app.use('*')` cheamă `requireAdmin` → `requireInstanceAdmin` (`routes/admin.ts:22`) — un `tenant_admin` delegat primește 401. Ștergerea vine din garbage collector, prin retenție. Corect azi, dar apărat de o gardă de rută, nu de o politică: o rută nouă care îl expune fără acea gardă e o scurgere.",
    "zv_backup_integrity_checks": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. Coloane: backup_id, checksum_md5, is_valid — verifică un FIȘIER, nu date.",
    "zv_backup_schedules": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. Coloane: cron, retenție, destinație S3, notificări — configurație de instanță.",
    "zv_backup_uploads": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. ÎN PLUS: **niciun cod nu atinge acest tabel.** Creat și indexat în migrația 001, zero situri de citire sau scriere în tot repo-ul (verificat 2026-09-02). E fie o funcție neterminată, fie un rest — clasificarea de instanță e corectă, dar întrebarea reală e dacă ar trebui să existe.",
    "zv_backups": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. Coloane: filename, size_bytes, status, error, notes, created_by.",
    "zv_ddl_jobs": "VERIFICAT 2026-09-02: fără `tenant_id`. Coada de DDL; DDL-ul e la nivel de schemă, iar schema e comună.",
    "zv_doc_templates": "VERIFICAT: **zero interogari de rulare** in ambele repo-uri (trei referinte, toate de tip). Pare tabela moarta — o tabela moarta n-are granita de aparat. De confirmat.",
    "zv_erd_layouts": "VERIFICAT: `routes/erd-layout.ts:51` filtreaza pe `user_id`. E aranjamentul personal al unei diagrame a schemei, iar schema e a instantei.",
    "zv_extension_schedule_runs": "VERIFICAT: scris de `lib/runtime/cron-runner.ts:272`. Extensiile se instaleaza per INSTANTA, deci si rularile programate ale lor.",
    "zv_flow_dlq": "**Copil al lui `zv_flows`.** VERIFICAT 2026-09-02, ambele situri: `GET /dlq` face `innerJoin` pe `zv_flows` filtrat cu `tenantOf(c)`, cu comentariul care explică de ce; `POST /dlq/:id/retry` citește intrarea după id, apoi cere flow-ul cu `tenant_id = tenantOf(c)` și dă 404 — ștergerea vine după verificare. Rămâne un oracol de existență (mesaje diferite pentru „intrare inexistentă\" și „flow străin\"), nu acces la date.",
    "zv_flow_runs": "**Copil al lui `zv_flows`, fără `tenant_id` propriu.** RLS n-are coloană pe care să se lege. VERIFICAT 2026-09-02, toate cele ȘASE situri (documentul de clasificare menționa doar două): ambele citiri din `routes/flows.ts` (r.200, r.420) fac `innerJoin` pe `zv_flows` și filtrează `f.tenant_id = tenantOf(c)`; cele patru scrieri din `flow-executor.ts` operează pe propria rulare — un INSERT cu `flowId`-ul deja rezolvat de planificator, trei UPDATE-uri după `runId`-ul tocmai creat, niciunul după un id venit din cerere. Corect, dar fără plasă: un join uitat ar fi o citire între firme cu nimic în spate.",
    "zv_flow_steps": "**Copil al lui `zv_flows`.** VERIFICAT 2026-09-02: cele trei rute (`POST /:id/steps`, `PUT`/`DELETE .../steps/:stepId`) verifică flow-ul cu `tenantOf(c)`; `flow-executor.ts:538` filtrează pe `flow_id`-ul flow-ului deja rezolvat de planificator. Corect.",
    "zv_license_audit": "VERIFICAT: citit din `lib/extensions/extension-marketplace-routes.ts:208`. Licentierea e a instantei, ca si instalarea extensiilor.",
    "zv_media_shares": "**Copil al lui `zv_media_files`/`zv_media_folders`.** VERIFICAT 2026-09-02: izolarea NU e prin join, cum sugera formularea generică. Accesul public se face după `token`, care E capabilitatea, ca la `zv_invitations`; gestionarea (`listUserShares`, `revokeShare`) filtrează pe `created_by = userId`. Sigur, dar prin alt mecanism.",
    "zv_media_versions": "**Copil al lui `zv_media_files`.** VERIFICAT 2026-09-02 — și NU era corect. `listFileVersions` interoga tabela cu `file_id` din URL fără să atingă părintele; firma B primea numere de versiune, dimensiuni, numele încărcătorului și `storage_path` pentru un fișier pe care nu-l putea citi. Reparat prin `INNER JOIN zv_media_files` (zveltio-extensions#78). O politică NU e opțiunea: `purgeExpiredTrash` rulează din `flow-scheduler` fără GUC. Notă separată: acea purjare se limitează deja la firma implicită, fiindcă citește întâi părintele policed — defect real, nereparat.",
    "zv_migrations": "VERIFICAT 2026-09-02: fără `tenant_id`. Starea schemei instanței — schema e una singură, comună.",
    "zv_notifications": "VERIFICAT partial: unicii cititori gasiti sunt in `routes/admin/system-routes.ts:268,284,296` — suprafata de administrare a INSTANTEI. Consecvent cu nivelul de instanta; n-am urmarit daca vreo extensie scrie in ea.",
    "zv_page_block_types": "VERIFICAT: catalog citit de `content/pages/engine/editor.ts:113`. E lista tipurilor de blocuri pe care le stie editorul, nu continut al vreunei firme.",
    "zv_panels": "**Copil al unei tabele per-firmă (zv_dashboards), fără `tenant_id` propriu.** RLS n-are coloană pe care să se lege. VERIFICAT 2026-09-02, toate cele zece situri, toate în `routes/insights.ts`: șapte trec prin `zv_dashboards` filtrat cu `tenantOf(c)`; `PATCH /panels/:id` și `DELETE /panels/:id` caută doar după `id` și se sprijină în schimb pe `requireInstanceAdmin` (god sau admin din domeniul rădăcină — blochează un `tenant_admin` delegat); `GET /stats` numără pe toată instanța, tot în spatele aceleiași gărzi. Inconsecvent cu ruta de creare, care filtrează `tenant_id` — dar nu e o scurgere, fiindcă garda restrânge la operatorul instanței.",
    "zv_pitr_config": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. Coloane: wal_archive_path, retention_days, last_wal_segment — WAL-ul e al clusterului.",
    "zv_pitr_restore_points": "VERIFICAT 2026-09-02. Nicio coloană nu identifică o firmă, iar conținutul e intrinsec al instanței. Nu mai e „clasificare plauzibilă\": un backup ESTE al întregii baze — `pg_dump` refuză un tabel ale cărui rânduri nu le poate dovedi complete, iar un rol care poate citi tot cere `BYPASSRLS` + `pg_read_all_data` (măsurat, ambele necesare). Toate cele 21 de rute din `routes/backup.ts` stau în spatele unei gărzi GLOBALE `router.use('*')` cu `requireInstanceAdmin` — god sau admin din domeniul rădăcină, deci un `tenant_admin` delegat primește 403. Coloana `lsn` e o poziție în WAL: un punct în timp pentru TOATĂ baza, care nu se poate restaura pentru o singură firmă.",
    "zv_push_subscriptions": "VERIFICAT: `routes/notifications.ts:257` filtreaza pe `user_id` si `endpoint`. Acelasi rationament ca `zvd_push_tokens`.",
    "zv_rag_documents": "VERIFICAT: doua referinte in tot codul ambelor repo-uri, niciuna o interogare de rulare. Pare tabela nefolosita — de confirmat inainte de a i se atribui o parte. O tabela moarta n-are granita de aparat.",
    "zv_rate_limit_configs": "VERIFICAT: citit si scris doar din `routes/admin/config-routes.ts:26,58` — suprafata de administrare a INSTANTEI. (Cota PER FIRMA e alt mecanism: `max_api_calls_day` din `zv_tenants`.)",
    "zv_request_logs": "VERIFICAT 2026-09-02, și e cel care merita privit: **poartă date care identifică o firmă** — `path` (deci ce colecții există), `user_id`, `ip`, `user_agent`. N-are `tenant_id`, deci RLS n-are pe ce se lega. Singurii cititori sunt `/api/admin/logs` și `/api/admin/status`, montate sub `adminRoutes`, a cărei gardă globală `app.use('*')` cheamă `requireAdmin` → `requireInstanceAdmin` (`routes/admin.ts:22`). Un `tenant_admin` delegat primește 401. Scris de middleware-ul de jurnalizare pe fiecare `/api/*`; șters de garbage collector prin retenție. Corect ca telemetrie de instanță — dar dacă vreo rută nouă îl expune fără acea gardă, e o scurgere, fiindcă nimic din bază nu o oprește.",
    "zv_roles": "VERIFICAT 2026-09-02: fără `tenant_id`. DEFINIȚIILE rolurilor sunt ale instanței; ATRIBUIREA lor e per firmă și trăiește în `zv_tenant_users`, care are `tenant_id` — confirmat în catalog, deci separarea revendicată aici chiar există în altă parte.",
    "zv_schema_branches": "VERIFICAT 2026-09-02: fără `tenant_id`. Ramurile de schemă sunt medii preview ale instanței.",
    "zv_schema_versions": "VERIFICAT 2026-09-02: fără `tenant_id`. Vezi `zv_migrations`.",
    "zv_settings": "VERIFICAT 2026-09-02: fără `tenant_id`. Configurația instanței.",
    "zv_slow_queries": "VERIFICAT 2026-09-02. Aceeași formă ca `zv_request_logs` și cu un risc în plus: coloana `query_params` poate conține valori de filtru, identificatori, termeni de căutare. Fără `tenant_id`, deci fără plasă în bază. Scris de `middleware/slow-query.ts` — al cărui comentariu spune deja „`zv_slow_queries` carries no `tenant_id`\" — și citit doar de `/api/admin/*`, în spatele aceleiași gărzi `requireInstanceAdmin`. Aceeași concluzie și aceeași fragilitate: corect azi, apărat de o gardă de rută, nu de o politică.",
    "zv_tenant_transfers": "VERIFICAT 2026-09-02: fără `tenant_id`. O mutare ÎNTRE firme are două firme, deci nu se poate lega de una singură.",
    "zv_tenants": "VERIFICAT 2026-09-02: fără `tenant_id`, și nu poate avea — e registrul firmelor, s-ar referi la sine. Ierarhia se face prin `parent_id`, iar vizibilitatea prin `zveltio_visible_tenants()`.",
    "zv_validation_rules": "VERIFICAT: `lib/validation-engine.ts:174` filtreaza pe `collection`, iar colectiile sunt ele insele partajate intre firme. O regula de validare pe o colectie partajata e coerent de instanta.",
    "zvd_branch_review_requests": "VERIFICAT: **zero interogari de rulare** in ambele repo-uri. Ramurile de schema sunt oricum de instanta, dar tabela pare nefolosita — de confirmat inainte sa i se atribuie o parte. Ruda cu `zv_rag_documents` si `zv_doc_templates`.",
    "zvd_collections": "VERIFICAT 2026-09-02: fără `tenant_id`, `rls=false`, zero politici — deci definițiile de colecții sunt PARTAJATE, nu o scăpare. Modelul e schemă comună + rânduri per firmă: tabelele de DATE (`zvd_<nume>`) primesc `applyTenantRLS` cu FORCE RLS, verificat azi. O instalare cu 100 de firme are un singur set de definiții și 100 de seturi de rânduri.",
    "zvd_column_permissions": "VERIFICAT 2026-09-02: fără `tenant_id`. Permisiuni pe coloanele unor definiții partajate. Vezi `zvd_collections`.",
    "zvd_dashboard_shares": "**Copil al lui `zv_dashboards`.** VERIFICAT 2026-09-02: se ajunge la el doar prin rute care au adus întâi tabloul de bord filtrat cu `tenantOf(c)` (`GET /dashboards/:id/shares`, `DELETE .../shares/:shareId`), iar `canReadDashboard` primește obiectul deja adus. Corect.",
    "zvd_dashboard_subscriptions": "**Copil al lui `zv_dashboards`.** VERIFICAT 2026-09-02: izolarea e pe UTILIZATOR, nu prin join — `GET /subscriptions` și `DELETE /subscriptions/:id` filtrează pe `user_id = user.id`. Un utilizator membru în două firme își vede abonamentele din amândouă, ceea ce e corect fiindcă sunt ale lui.",
    "zvd_panel_cache": "VERIFICAT: cache-ul se citeste DUPA `canReadDashboard(db, dash, user.id)` (`routes/insights.ts:592`) si e cheiat pe `panel_id`, iar un panou apartine unui dashboard per-firma. Izolarea tine prin autorizare, nu prin coloana — aceeasi clasa ca cele noua.",
    "zvd_permissions": "VERIFICAT 2026-09-02, prin numărare pe baza vie: **155 de reguli `p`, toate 155 cu `dom='*'`**. Depozitul Casbin e global prin construcție, iar domeniul per firmă vine din `getCurrentDomain()` la evaluare, nu din rând. Asta a și infirmat premisa că politicile cresc cu numărul de firme.",
    "zvd_push_tokens": "VERIFICAT: `routes/notifications.ts:126` filtreaza pe `user_id`. Un jeton de dispozitiv apartine unui utilizator, iar utilizatorii apartin mai multor firme — deci nu are firma.",
    "zvd_relations": "VERIFICAT 2026-09-02: fără `tenant_id`. Relații ÎNTRE definiții de colecții, deci la același nivel ca ele. Vezi `zvd_collections`.",
    "zvd_rls_policies": "VERIFICAT 2026-09-02: fără `tenant_id`. Regulile de rând se DEFINESC pe colecții partajate și se APLICĂ per firmă, prin GUC. Vezi `zvd_collections`.",
    "zvd_rpc_functions": "VERIFICAT 2026-09-02: fără `tenant_id`. Funcții definite la nivel de instanță, ca și colecțiile."
  },
  "_unpoliced": "Tabele care POARTĂ `tenant_id` și nu au politică RLS, declarat deliberat. Coloana marchează rândul; doar politica îl apără. O intrare nouă înseamnă o decizie, nu o omisiune — două omisiuni au trecut drept decizii până când poarta a început să întrebe (workflow/checklists 006, ai 008).",
  "unpoliced": {
    "zv_tenant_users": "`getUserTenants()` (`tenant-manager.ts:445`) răspunde la „în ce firme sunt?\" — întrebarea de DINAINTE de a alege firma. Cu politică, lista de firme la login e goală și nimeni nu mai poate comuta.",
    "zv_api_keys": "`validateApiKey()` (`lib/data/auth.ts:96`) caută DOAR după hash; cheia e cea care STABILEȘTE firma. Comparația cu firma cerută vine imediat după (`auth.ts:119`). Cu politică, autentificarea prin cheie API cade în întregime.",
    "zv_invitations": "Răscumpărată după token, de un neautentificat care încă nu e membru nicăieri (`routes/auth.ts:90,123`). `/api/auth` e în `TXN_SKIP_PREFIXES`, deci nici nu există tranzacție de firmă. Tokenul E capabilitatea.",
    "zv_environments": "`resolveEnvironment()` e chemată DIN INTERIORUL lui `tenantMiddleware`, înainte ca `withTenantIsolation` să deschidă tranzacția (`middleware/tenant.ts:75`). O politică aici rupe rezolvarea firmei — adică tot. Filtrează explicit pe `tenant_id`.",
    "zv_tenant_usage": "Scrisă de `tenant-quota.ts:136` pe `quotaDb = poolDb ?? db` — pool, în afara tranzacției cererii, prin construcție. Citită agregat pe instanță de `/api/admin/system`. E registrul contabil al instanței.",
    "zv_extension_registry": "Citită la boot (`index.ts:532,561`), fără nicio cerere. Extensiile se instalează per INSTANȚĂ, nu per firmă.",
    "zv_dashboards": "`insightsRoutes(poolDb, auth)` (`routes/index.ts:458`) — pe pool, deliberat: ruta pune `SET TRANSACTION READ ONLY` și `statement_timeout` proprii, care n-au ce căuta pe toată cererea. CORECTAT 2026-09-02: motivul de dinainte spunea „filtrează manual, cu `tenantOf(c)`, la FIECARE acces\" — verificat rută cu rută, e fals. Zece din treisprezece filtrează. Trei nu: `GET /stats` (numărători pe instanță), `PATCH /panels/:id` și `DELETE /panels/:id`. Acelea sunt apărate de `requireInstanceAdmin`, care e un control DIFERIT și suficient: cere rolul god SAU grantul de admin venit din domeniul rădăcină, deci un `tenant_admin` delegat al altei firme primește 403 (`permissions.ts:812`, cu motivul în comentariu). Nu e scurgere; e o a doua linie, nu aceeași linie.",
    "zv_flows": "`flowsRoutes(poolDb, auth)` (`routes/index.ts:482`), plus `flow-scheduler.ts:90` care deschide propria tranzacție fără `SET LOCAL ROLE` și fără GUC — trebuie să vadă flow-urile scadente ale TUTUROR firmelor, altfel nu le poate rula.",
    "zv_revisions": "`afterWrite` scrie jurnalul pe pool, și codul o spune (`write-pipeline.ts:411`): „afterWrite runs on the pool, not the request transaction, so it can't rely on the RLS GUC\". Pune `tenant_id` explicit.",
    "zvd_webhooks": "Migrația 016 a adăugat `tenant_id` tocmai ca să repare un IDOR între firme, și a ales izolare în aplicație: `routes/webhooks.ts` rulează pe pool, iar `WebhookManager.trigger()` e chemat din `afterWrite`, tot pe pool, cu firma trecută explicit. `repairUnsignedWebhooksAtBoot` (`lib/webhooks.ts:36`) trebuie să vadă webhook-urile tuturor firmelor la pornire.",
    "zvd_webhook_deliveries": "Copil al lui `zvd_webhooks`, scris de același expeditor de pe pool. Vezi mai sus."
  }
}
